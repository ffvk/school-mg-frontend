import { InterceptorProviders } from './interceptor-providers';

describe('InterceptorProviders', () => {
  it('should create an instance', () => {
    expect(new InterceptorProviders()).toBeTruthy();
  });
});
