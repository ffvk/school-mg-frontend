import { UpdateHomeworkDTO } from './update-homework-dto';

describe('UpdateHomeworkDTO', () => {
  it('should create an instance', () => {
    expect(new UpdateHomeworkDTO()).toBeTruthy();
  });
});
